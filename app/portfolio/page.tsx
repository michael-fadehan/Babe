import Gallery from '../components/Gallery';

export const metadata = {
  title: 'Portfolio — Derin in Details',
};

export default function PortfolioPage() {
  return (
    <main className="page-shell">
      <Gallery />
    </main>
  );
}
